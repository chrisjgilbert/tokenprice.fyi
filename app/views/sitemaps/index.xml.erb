<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc><%= root_url %></loc></url>
  <url><loc><%= trends_url %></loc></url>
  <url><loc><%= compare_url %></loc></url>
  <url><loc><%= sources_url %></loc></url>
  <url><loc><%= learn_url %></loc></url>
  <url><loc><%= learn_anatomy_url %></loc></url>
  <url><loc><%= how_pricing_works_url %></loc></url>
  <url><loc><%= learn_reasoning_url %></loc></url>
  <url><loc><%= learn_feature_costs_url %></loc></url>
  <url><loc><%= learn_cost_cutting_url %></loc></url>
  <url><loc><%= guide_url %></loc></url>
  <% FeaturePattern.all.each do |pattern| %>
    <url><loc><%= guide_task_url(pattern.slug) %></loc></url>
  <% end %>
  <% @providers.each do |provider| %>
    <url><loc><%= provider_url(provider) %></loc></url>
  <% end %>
  <% @models.each do |model| %>
    <url>
      <loc><%= model_url(model) %></loc>
      <% if model.released_on %><lastmod><%= model.released_on.iso8601 %></lastmod><% end %>
    </url>
  <% end %>
</urlset>
