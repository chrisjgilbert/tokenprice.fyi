<%# Live "prices synced" status. Shown in the desktop bar and the mobile drawer
    footer; `css` swaps the wrapper class for each context. %>
<span class="<%= local_assigns.fetch(:css, "tp-nav-sync") %>"><span class="tp-nav-dot"></span>prices synced <span class="num"><%= Date.current.iso8601 %></span></span>
