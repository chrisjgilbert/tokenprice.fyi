<nav class="mb-4 text-sm text-slate-500"><%= link_to "← #{@model.name}", edit_admin_model_path(@model), class: "hover:text-slate-900" %></nav>
<h1 class="mb-1 text-2xl font-bold tracking-tight">Add price</h1>
<p class="mb-6 text-sm text-slate-500"><%= @model.name %> · <%= @model.provider.name %></p>
<%= render "form", model: @model, price_point: @price_point %>
