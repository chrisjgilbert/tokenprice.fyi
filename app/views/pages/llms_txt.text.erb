# llms.txt — tokenprice.fyi
# https://llmstxt.org/

# tokenprice.fyi is an independent LLM API pricing tracker.
# It records input, output, and cached token prices for 40+ models across 12 providers,
# updated daily, with full price history since launch.

> tokenprice.fyi: LLM API price tracker. Input, output, and cached token prices for Claude, GPT, Gemini, Grok, DeepSeek and more. Updated daily with full price history.

## Pages

- Pricing table (all models): <%= root_url %>
- Compare two models side by side: <%= compare_url %>
- AI pricing news feed (releases, price changes, market moves): <%= news_url %>
- Market events & price change timeline: <%= events_url %>
- Flagship prices over time (every provider's top model, priced at launch): <%= trends_url %>
- Data sources & methodology: <%= sources_url %>
- JSON API (pricing data): <%= api_v1_models_url(format: :json) %>

## Providers
<% @providers.each do |provider| %>
- <%= provider.name %>: <%= provider_url(provider) %>
<% end %>

## Learn — explainers on LLM API pricing

- How LLM pricing works: <%= how_pricing_works_url %>
- What drives the cost of common features: <%= learn_feature_costs_url %>
- Cost-cutting strategies: <%= learn_cost_cutting_url %>
- Reasoning & thinking tokens: <%= learn_reasoning_url %>
