<% content_for :title, "Which model should you actually use? — tokenprice.fyi" %>
<% content_for :description, "A task-by-task guide to picking an LLM tier: classification, extraction, RAG, summarisation, and agentic coding — and which price dimension actually dominates each." %>

<% content_for :head do %>
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Article",
      "headline": "Which model should you actually use?",
      "datePublished": "2026-06-11",
      "author": { "@type": "Organization", "name": "tokenprice.fyi" },
      "publisher": { "@type": "Organization", "name": "tokenprice.fyi" },
      "mainEntityOfPage": "<%= which_model_url %>"
    }
  </script>
<% end %>

<style>
  .why-article { max-width: 680px; margin: 0 auto; padding: 24px 0 48px; }
  .why-article .why-meta { font-family: var(--font-mono); font-size: 13px; color: var(--color-slate-400); margin-bottom: 14px; }
  .why-article h1 { font-size: 38px; font-weight: 600; letter-spacing: -0.03em; line-height: 1.15; color: var(--color-slate-900); margin-bottom: 28px; }
  .why-article h2 { font-size: 22px; font-weight: 600; letter-spacing: -0.02em; color: var(--color-slate-900); margin: 34px 0 12px; }
  .why-article p { font-size: 16.5px; line-height: 1.7; color: var(--color-slate-700); margin-bottom: 20px; }
  .why-article strong { color: var(--color-slate-900); }
  .why-article a { color: var(--color-slate-900); text-decoration: underline; text-underline-offset: 3px; text-decoration-color: var(--color-slate-300); }
  .why-article a:hover { text-decoration-color: var(--color-slate-900); }
  .why-article .num { font-family: var(--font-mono); }
  .why-article .verdict { font-family: var(--font-mono); font-size: 13px; color: var(--color-slate-500); margin: -6px 0 10px; }

  /* Plain-language cheat-sheet for people choosing from a dropdown */
  .wm-tldr {
    background: var(--color-slate-50);
    border: 1px solid var(--color-slate-200);
    border-radius: var(--radius-md);
    padding: 22px 24px 8px;
    margin: 0 0 32px;
  }
  .wm-tldr-label {
    font-family: var(--font-mono); font-size: 11px; font-weight: 600;
    text-transform: uppercase; letter-spacing: 0.08em;
    color: var(--color-slate-400); margin-bottom: 14px;
  }
  .wm-tldr-intro { font-size: 14.5px; line-height: 1.6; color: var(--color-slate-600); margin: 0 0 18px; }
  .wm-tier { display: flex; gap: 12px; align-items: baseline; margin-bottom: 14px; }
  .wm-tier-dot { width: 9px; height: 9px; border-radius: 50%; flex: none; transform: translateY(2px); }
  .wm-tier-small   .wm-tier-dot { background: #059669; }
  .wm-tier-mid     .wm-tier-dot { background: #d97706; }
  .wm-tier-frontier .wm-tier-dot { background: #e11d48; }
  .wm-tier-name { font-weight: 600; color: var(--color-slate-900); font-size: 15px; min-width: 78px; flex: none; }
  .wm-tier-body { font-size: 14.5px; line-height: 1.55; color: var(--color-slate-600); margin: 0; }
  .wm-tier-body strong { color: var(--color-slate-900); }
  .wm-tldr-foot { font-size: 13.5px; line-height: 1.6; color: var(--color-slate-500); border-top: 1px solid var(--color-slate-200); padding: 14px 0 12px; margin-top: 6px; }

  @media (max-width: 640px) {
    .why-article h1 { font-size: 30px; }
    .wm-tier { flex-wrap: wrap; gap: 4px 10px; }
    .wm-tier-name { min-width: 0; }
  }
</style>

<article class="why-article">
  <p class="why-meta">June 2026</p>
  <h1>Which model should you actually use?</h1>

  <div class="wm-tldr">
    <p class="wm-tldr-label">The short version</p>
    <p class="wm-tldr-intro">Almost every model lineup is three tiers. The names change; the trade-off doesn't:</p>
    <div class="wm-tier wm-tier-small">
      <span class="wm-tier-dot"></span>
      <span class="wm-tier-name">Small</span>
      <p class="wm-tier-body"><strong>Fast and cheap.</strong> Haiku, GPT-mini, Flash. Good for quick edits, search, simple Q&amp;A, and high-volume routine work. Underused.</p>
    </div>
    <div class="wm-tier wm-tier-mid">
      <span class="wm-tier-dot"></span>
      <span class="wm-tier-name">Mid</span>
      <p class="wm-tier-body"><strong>The all-rounder.</strong> Sonnet, GPT standard, Gemini Pro. Everyday coding, writing, RAG. Handles most tasks at a fraction of frontier prices.</p>
    </div>
    <div class="wm-tier wm-tier-frontier">
      <span class="wm-tier-dot"></span>
      <span class="wm-tier-name">Frontier</span>
      <p class="wm-tier-body"><strong>Capable, slower, expensive.</strong> Fable, Opus, GPT's reasoning models. Worth it for hard, multi-step work — agentic coding, difficult debugging, deep analysis — where marginal quality gains per step compound. Often <span class="num">10×</span> the cost of a small model. Overkill for most tasks.</p>
    </div>
    <p class="wm-tldr-foot">Rule of thumb: <strong>start one tier lower than feels comfortable, and only move up when the cheaper model actually falls short.</strong> The rest of this page is the longer version — task by task — and <%= link_to "the price table", root_path %> shows what each tier costs.</p>
  </div>

  <p>Using a frontier model for everything is a common and costly default. <%= link_to "The reasoning is understandable", why_path %>, but the price spread is large: within Anthropic's lineup alone, Claude Fable 5 costs <span class="num">10×</span> what Claude Haiku 4.5 does on every dimension; across providers, the gap between the priciest frontier and the cheapest small model exceeds <span class="num">300×</span>. A tier mismatch isn't a rounding error. At volume, it can dominate the bill.</p>

  <p>A simple rule works well: <strong>use the cheapest model that passes your eval, and route up when it fails.</strong> With no eval yet, run it in reverse — prototype on a frontier model to find the quality ceiling, then walk down until something breaks. The eval is the arbiter, not this page and not a leaderboard. Below: the five most common task shapes, the tier that usually suffices, and which price dimension tends to dominate — often not the one on the sticker.</p>

  <h2>Classification and routing</h2>
  <p class="verdict">tier: small · cost driver: input price</p>
  <p>Labelling tickets, moderating content, routing requests — a paragraph or a page in, a token or two out. This is the task small models are built for, and providers themselves position their smallest models for classification and routing. (A large label taxonomy or subtle multi-label calls can push the choice up a tier — that's what the eval is for.) Because output is near-zero, <strong>input price is effectively the entire bill</strong>, and small-tier input starts around <span class="num">$0.10</span> per million tokens. A frontier model here adds cost without adding much that the task needs.</p>

  <h2>Data extraction to JSON</h2>
  <p class="verdict">tier: small–mid · cost driver: input price</p>
  <p>Pulling structured fields out of messy text. Small models handle typical schemas; stepping up to mid helps when the source is adversarial or a missed field is expensive. Deep nesting is a separate problem — it trips up <em>every</em> tier, so it's usually better to simplify the schema than to upgrade the model. The metric that matters here is <strong>cost per accepted document, not cost per request</strong>: a cheap model that fails twice and needs human review isn't really cheap. Output is short and mechanical, so input price dominates again — and extraction is a classic candidate for <strong>batch pricing</strong>, which halves the bill at most providers when results can wait minutes instead of seconds.</p>

  <h2>RAG answering</h2>
  <p class="verdict">tier: mid · cost driver: input price, then cached input</p>
  <p>Answering questions over retrieved documents. The retrieval does the hard part; the model mostly needs to read carefully and decline to invent. Mid-tier models are reliably good enough here, and the economics are lopsided: thousands of tokens of context go in to get a paragraph back, so <strong>the spend is overwhelmingly on input tokens</strong>. If the system prompt and document set repeat across requests, cached-input pricing — typically <span class="num">4–10×</span> cheaper than regular input — can change which model comes out cheapest.</p>

  <h2>Long-document summarisation</h2>
  <p class="verdict">tier: small–mid · cost driver: input price</p>
  <p>The most input-heavy shape there is: a hundred pages in, one out — so tier choice gets multiplied by every page fed in. Cheap long-context models have made routine summarisation a small-tier task; mid or above is worth paying for when missing a clause in the middle of page sixty actually carries a cost — "lost in the middle" is reduced, not solved. And because summarisation is rarely latency-sensitive, it's another natural batch-pricing candidate.</p>

  <h2>Agentic coding</h2>
  <p class="verdict">tier: frontier · cost driver: cached input, then output</p>
  <p>This is the main exception. Multi-step agents that read a codebase, plan, edit, and re-check compound their errors — a model that's 95% as good per step is far less than 95% as good after thirty steps. This is the task where frontier prices tend to pay off. (For the main loop, at least — an emerging pattern is a frontier orchestrator delegating search and exploration to small-tier subagents.) One detail is easy to overlook: agents re-send the same growing context on every step, so most of the spend goes on cache reads, and <strong>cached-input price matters more than headline input price</strong>. Two frontier models with the same sticker can differ sharply once caching enters the maths — it's worth checking the cached column on <%= link_to "the table", root_path %>, not just the first one.</p>

  <h2>Caveats</h2>
  <p>These are dated judgments, not benchmarks — they say "good enough for typical schemas," never "scores 87.2," and when the market moves (a mid model gets frontier-grade at coding, say) these verdicts get revised, visibly. The tier floor is also only half the decision: a "small" verdict still leaves more than a <span class="num">10×</span> spread between the cheapest and priciest small models, which is what <%= link_to "the table", root_path %> is for.</p>

  <p>The other half of the decision — what does mid-tier RAG actually cost at your volume? — is what <%= link_to "the estimator", cost_path %> is for. Pick a task shape, set your volume, and get a ranked monthly bill across every tracked model at live prices, shareable as a link. Start cheap, write an eval, and only pay frontier prices where the task earns them.</p>
</article>
