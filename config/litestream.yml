dbs:
  - path: /rails/storage/production.sqlite3
    replicas:
      - type: s3
        endpoint: https://fsn1.your-objectstorage.com
        bucket: tokenprice-backups
        path: db/production
        access-key-id: ${LITESTREAM_ACCESS_KEY_ID}
        secret-access-key: ${LITESTREAM_SECRET_ACCESS_KEY}
        force-path-style: true

  - path: /rails/storage/production_cache.sqlite3
    replicas:
      - type: s3
        endpoint: https://fsn1.your-objectstorage.com
        bucket: tokenprice-backups
        path: db/production_cache
        access-key-id: ${LITESTREAM_ACCESS_KEY_ID}
        secret-access-key: ${LITESTREAM_SECRET_ACCESS_KEY}
        force-path-style: true

  - path: /rails/storage/production_queue.sqlite3
    replicas:
      - type: s3
        endpoint: https://fsn1.your-objectstorage.com
        bucket: tokenprice-backups
        path: db/production_queue
        access-key-id: ${LITESTREAM_ACCESS_KEY_ID}
        secret-access-key: ${LITESTREAM_SECRET_ACCESS_KEY}
        force-path-style: true

  - path: /rails/storage/production_cable.sqlite3
    replicas:
      - type: s3
        endpoint: https://fsn1.your-objectstorage.com
        bucket: tokenprice-backups
        path: db/production_cable
        access-key-id: ${LITESTREAM_ACCESS_KEY_ID}
        secret-access-key: ${LITESTREAM_SECRET_ACCESS_KEY}
        force-path-style: true
